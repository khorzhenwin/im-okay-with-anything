import { Text } from "@mantine/core";

const SimpleElection = ({ theme }: { theme: string }) => {
  return (
    <div>
      <Text>This is a simple election component</Text>
    </div>
  );
};

export default SimpleElection;